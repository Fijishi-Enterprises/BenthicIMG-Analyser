Django==1.4

# Database migrations
South==0.7.6

# Per-object permissions
# e.g. "can edit this source" not just "can edit sources"
django-guardian==1.1.0b0

# System for generating smaller versions of images
easy-thumbnails==1.2

# User management system (going beyond Django's built-in facilities)
django-userena==1.0.1

# Versioning of database objects
# Has a fine-grained Django compatibility table; 1.6.1 is for Django 1.4
django-reversion==1.6.1

# Fancier error log view
django-sentry==1.12.2

# Backend asynchronous task management
django-celery==3.0.17
django-supervisor==0.3.1

# Allows Django to connect to a PostgreSQL database
psycopg2==2.6.1

# Statistics charts
GChartWrapper==0.9

# Fancy math
numpy==1.7.1

# Python Imaging Library
# (A currently active, package friendly fork of it; also endorsed by Django)
pillow==3.0.0

# Handle the YAML format, e.g. for fixtures
PyYAML==3.10

# More elegant filepath calculations
unipath==1.1
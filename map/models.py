# All of our apps must have a models.py so that our
# "run tests for every app" command doesn't get this kind of error:
# "ImproperlyConfigured: App with label map could not be found"
# (As of Django 1.3.0)